<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.LayoutTrialApp" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/dialog_background</item>
        <item name="colorPrimaryVariant">@color/dialog_text_color</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="colorOnPrimary">@color/purple_200</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>


    <!--    Dialog Box -->
    <style name="AlertDia" parent="Theme.AppCompat.DayNight.Dialog.Alert">
        <item name="android:background">@android:color/transparent</item>
        <item name="android:shadowColor">@android:color/transparent</item>
        <item name="android:bottomLeftRadius">30dp</item>
        <item name="android:bottomRightRadius">30dp</item>
        <item name="android:topLeftRadius">30dp</item>
        <item name="android:topRightRadius">30dp</item>
        <item name="android:textColor">@color/dialog_text_color</item>
    </style>
    <style name="AlertDia.dialogButton" parent="AlertDia">
        <item name="android:background">@android:color/transparent</item>
        <item name="android:bottomLeftRadius">30dp</item>
        <item name="android:bottomRightRadius">30dp</item>
        <item name="android:topLeftRadius">30dp</item>
        <item name="android:topRightRadius">30dp</item>
        <item name="android:textColor">#ffffff</item>
    </style>
</resources>